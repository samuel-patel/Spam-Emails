# Spam-Emails
A LSTM neural network trained using a public dataset to classify spam emails. The jupyter notebook file contains the code for the classifier and preprocessing along with plots and brief justifications for my approach.

Command to install required packages:
```
python -m pip install tensorflow pandas matplotlib nltk scikit-learn imblearn
```
