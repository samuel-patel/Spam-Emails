# Spam-Emails
A LSTM neural network trained using the spam vs ham dataset to classify spam emails. The jupyter notebook file contains the code for the classifier and preprocessing along with plots and brief justifications for my approach.
